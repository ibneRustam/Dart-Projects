void main() {
  int yob = 2000;
  int currentYear = 2025;

  // var result = currentYear - yob;

  for (int res = currentYear - yob; res > 4; res ++ ) ;
  {
    print("Year");
  }

  // int add = brithdayYear + currentYear;

  // for (brithdayYear = 0; brithdayYear < currentYear; add + 4) ;
  // {
  //   print("No $brithdayYear");
  // }

  // num result = addYear % 4;
  // print(result);
}
